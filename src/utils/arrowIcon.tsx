import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faAngleDown } from '@fortawesome/free-solid-svg-icons';

const Arrow = () => {
  return (
    <div>
      <FontAwesomeIcon icon={faAngleDown} size={'1x'} color={'grey'}/>
    </div>
  );
};

export default Arrow;
